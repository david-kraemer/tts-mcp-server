[project]
name = "tts-mcp-server"
version = "0.1.0"
description = "TTS notification server for Claude Code"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "mlx-audio>=0.2.8",
    "soundfile>=0.12.1",
]
